name: Build and Publish Release

on:
  push:
    tags:
      - '*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v0.2.0)'
        required: true
        type: string

env:
  REGISTRY: docker.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  verify-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Verify version
        env:
          GITHUB_REF_NAME: ${{ inputs.tag || github.ref_name }}
        run: uv run python .github/scripts/verify_tag_matches_pyproject_version.py

  ci:
    uses: ./.github/workflows/continuous-integration.yaml
    with:
      run-sonar: true
      run-dependency-check: true
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  publish-image:
    needs:
      - ci
      - verify-version
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Extract version from tag
        id: version
        run: |
          VERSION=${{ inputs.tag || github.ref_name }}
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # - name: Install Cosign
      #   uses: sigstore/cosign-installer@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}},value=${{ steps.version.outputs.VERSION }}
            type=semver,pattern={{major}}.{{minor}},value=${{ steps.version.outputs.VERSION }}
            type=semver,pattern={{major}},value=${{ steps.version.outputs.VERSION }}
            type=raw,value=latest

      - name: Build and push Docker image
        id: build-push
        uses: docker/build-push-action@v6
        with:
          context: .
          # platforms: linux/amd64,linux/arm64,linux/arm/v7
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: true
          sbom: true

      # - name: Sign Docker images
      #   env:
      #     DIGEST: ${{ steps.build-push.outputs.digest }}
      #     TAGS: ${{ steps.meta.outputs.tags }}
      #   run: |
      #     echo "${TAGS}" | xargs -I {} cosign sign --yes {}@${DIGEST}

  publish-package:
    needs:
      - ci
      - verify-version
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version-file: .python-version

      - name: Build Python package
        run: uv build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # with:
        #   username: __token__
        #   password: ${{ secrets.PYPI_API_TOKEN }}

  # github-release:
  #   needs: [publish-image, publish-package]
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: write

  #   steps:
  #     - uses: actions/checkout@v6
  #       with:
  #         fetch-depth: 0

  #     - name: Extract version from tag
  #       id: version
  #       run: |
  #         VERSION=${GITHUB_REF#refs/tags/}
  #         echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

  #     - name: Generate changelog
  #       id: changelog
  #       run: |
  #         PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
  #         if [ -z "$PREV_TAG" ]; then
  #           CHANGELOG=$(git log --pretty=format:"- %s (%h)" ${{ steps.version.outputs.VERSION }})
  #         else
  #           CHANGELOG=$(git log --pretty=format:"- %s (%h)" ${PREV_TAG}..${{ steps.version.outputs.VERSION }})
  #         fi
  #         echo "CHANGELOG<<EOF" >> $GITHUB_OUTPUT
  #         echo "$CHANGELOG" >> $GITHUB_OUTPUT
  #         echo "EOF" >> $GITHUB_OUTPUT

  #     - name: Create GitHub Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         tag_name: ${{ steps.version.outputs.VERSION }}
  #         name: Release ${{ steps.version.outputs.VERSION }}
  #         body: |
  #           ## What's Changed

  #           ${{ steps.changelog.outputs.CHANGELOG }}

  #           ## Docker Images

  #           - `${{ env.IMAGE_NAME }}:${{ steps.version.outputs.VERSION }}`
  #           - `${{ env.IMAGE_NAME }}:latest`

  #           **Platforms:** linux/amd64, linux/arm64, linux/arm/v7

  #           ## PyPI

  #           ```bash
  #           pip install shine2mqtt==${{ steps.version.outputs.VERSION }}
  #           ```
  #         draft: false
  #         prerelease: false
